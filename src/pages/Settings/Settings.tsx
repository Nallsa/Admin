import { Box } from '@mui/material';
import ProductTypes from '../../components/settings/productTypes';

const Settings = () => {
  return (
    <Box>
      <ProductTypes />
    </Box>
  );
};

export default Settings;
